#include "main.hpp"
#include <iostream>

using namespace std;

int main()
{
    CityTest city = CityTest();
    if (country.run() == Main::failure) {
        cout<< "Erro no teste de pais";
        return Main::failure
    }

    cout<<"Testes finalizados";

    return Main::success;
}