#include "accommodation.hpp"

void Accommodation::setCity(string city) {
    this->city.setCity(city);
}

void Accommodation::setCode(string code) {
    this->code.setCode(code);
}


void Accommodation::setGrade(int grade) {
    this->grade.setGrade(grade);
}

void Accommodation::setDescription(string description) {
    this->description.setDescription(description);
}
